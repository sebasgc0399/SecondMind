# SPEC — F42: Polish offline UX y visibilidad per-item (Registro de implementación)

> Estado: Completada Mayo 2026 — mergeada a main (`c7247de`) y liberada en 0.2.9 (`53a6f6b`).
> Commits: `79b5854` F42.4 inbox "En cola" offline, `16551f7` F42.5 avatar fallback, `6f908cf` F42.1 hook + dot per-item en 5 cards, `059071d` F42.2 dialog descarte, `60ea80d` F42.3 paridad cross-platform (deprecar OfflineBadge), `18f3a38` polish del dialog, `c7247de` merge a main, `53a6f6b` release 0.2.9.
> Gotchas operativos vigentes → `Spec/ESTADO-ACTUAL.md` (+ `Spec/gotchas/tinybase-firestore.md` per-entity hook, `Spec/gotchas/ui-componentes.md` img onError fallback)

## Objetivo

El usuario tenía un buen indicador agregado de "hay X cambios pendientes" (chip `<PendingSyncIndicator />` con popover en header mobile + sidebar desktop), pero le faltaba transparencia per-ítem y consistencia cross-platform: (1) items creados/editados offline sin marcador visual propio en su card, (2) inbox items capturados offline mostrando `Procesando con AI...` con spinner indefinido cuando la CF nunca corre offline, (3) divergencia entre mobile (chip top + popover) y desktop (`<OfflineBadge />` toast genérico bottom + chip). Adicionalmente, en el `<NavigationDrawer />` de Capacitor el avatar Google podía fallar a cargar y dejar el `alt` truncado dentro del círculo, generando layout shift.

F42 cerró estos cinco gaps de polish UX visible sin tocar el `saveQueue` ni el modelo de persistencia (eso vive en F28/F29/F30).

## Qué se implementó

- **F42.1 — Hook `usePendingSyncForEntity` + dot per-item:** hook nuevo con patrón cached-by-version de `usePendingSyncCount` (subscribe app-lifetime + `bumpVersion`) sobre el mapa `entityType → queues[]`; dot esquina superior-derecha (`bg-amber-500` pending / `bg-destructive` error) en las 5 cards listables. Composite keys de `saveNotesMetaQueue` (`${id}:accept-...`) matcheadas con `startsWith`. Limitación aceptada: items capturados via `QuickCaptureProvider.save` (setRow directo, fuera de queue) no se marcan. Archivos: `src/hooks/usePendingSyncForEntity.ts` (+test), `src/components/editor/NoteCard.tsx`, `src/components/capture/InboxItem.tsx`, `src/components/tasks/TaskCard.tsx`, `src/components/projects/ProjectCard.tsx`, `src/components/objectives/ObjectiveCard.tsx`.
- **F42.2 — Confirmación detallada antes de descartar:** reemplazó el `handleDiscardAll` directo por `<DiscardPendingDialog />` (base-ui Dialog) que lista los pending agrupados por entidad con labels resueltos del store local (snapshot al abrir, no reactivo); auto-cierre si la lista queda vacía mid-dialog. Helper `getDiscardableEntries()` en `saveQueue.ts` itera las queues y resuelve labels. Archivos: `src/components/layout/DiscardPendingDialog.tsx` (nuevo), `src/components/layout/PendingSyncIndicator.tsx`, `src/lib/saveQueue.ts`.
- **F42.3 — Paridad cross-platform (deprecar OfflineBadge):** unificó "estado de red + cambios pendientes" en `<PendingSyncIndicator />` con tres modos (online sin pending → nada; online con pending → chip ámbar; offline → chip cloud-off persistente con leyenda explicativa en popover). El contrato `if (!hasAny) return null` pasó a `if (isOnline && !hasAny) return null`; "Reintentar" disabled offline; triggerKey de re-expand ahora distingue `'normal' | 'error' | 'offline'`. `<OfflineBadge />` borrado. Archivos: `src/components/layout/PendingSyncIndicator.tsx` (+test), `src/components/layout/OfflineBadge.tsx` (borrado), `src/app/layout.tsx`.
- **F42.4 — Mensaje correcto de inbox offline:** branching `useOnlineStatus() && !item.aiProcessed` → "En cola — se procesará al reconectar" con icono cloud-off (sin spinner) en vez del engañoso "Procesando con AI...". Heurística válida porque la CF `onDocumentCreated` requiere doc en Firestore, que requiere red. Archivos: `src/components/capture/InboxItem.tsx`.
- **F42.5 — Fix avatar fallback en Sidebar (Capacitor):** `onError` handler que setea `imgError` (state local en `<SidebarContent />`), reseteado al cambiar `user.photoURL`; cuando `imgError` o `!photoURL` se renderiza solo el placeholder `<div>`, nunca ambos simultáneamente. Quedó local al `<SidebarContent />` (no requirió wrapper de shadcn). Archivos: `src/components/layout/Sidebar.tsx`.
- **Post-merge:** `18f3a38` suavizó el color destructive del dialog y eliminó la X redundante.

## Decisiones clave

| #   | Decisión                                                                                                  | Razón                                                                                                                                                                                                                                            |
| --- | --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| D1  | Badge per-item = dot esquina superior-derecha (vs outline en border o icono inline)                       | Mantiene la convención del indicador global; outline genera ruido en listas densas, icono inline pelea con tags de metadata. Dot es mínimo y consistente.                                                                                        |
| D2  | El hook chequea TODAS las queues de la entidad (`isPending` si cualquiera tiene entry no-synced)          | Notas pueden estar pending en content/meta/creates a la vez; tasks/projects/objectives en meta+creates; inbox solo en `saveInboxQueue`. Items via `QuickCaptureProvider` con setRow directo = cobertura parcial documentada.                     |
| D3  | Eliminar `<OfflineBadge />` y mover el rol al `<PendingSyncIndicator />` (vs dejar ambos o toast efímero) | Dos componentes para "estado de red + pending" generan inconsistencia y obligan a leer dos UIs. El chip ya está visible cross-platform en posición consistente con popover; agregarle el anuncio offline es low-cost y unifica el modelo mental. |
| D4  | Heurística offline-pending para inbox = `useOnlineStatus() && !item.aiProcessed` (sin consultar queues)   | La CF `processInboxItem` se trigger via `onDocumentCreated`, que requiere el doc en Firestore, que requiere red. Si se creó offline no hay doc aún → CF no corre. "En cola" es 100% preciso para ese estado.                                     |

## Lecciones

- **Per-entity selectors sobre un aggregator app-lifetime deben preservar la referencia con cache-by-version**, no re-suscribirse por entityId — el patrón de `usePendingSyncCount` (subscribe único + `bumpVersion` al notify) evita re-renders cuando cambia el status de OTRO id. Canon escalado a `Spec/gotchas/tinybase-firestore.md` (post-F42.1).
- **Composite keys en una queue (`${id}:accept-${suggestionId}`) requieren matching por `startsWith(\`${id}:\`) || key === id`**, no igualdad exacta, para que un selector per-entity encuentre todas las entries de un id.
- **`<img>` de fuente externa (Google profile pic en WebView nativo) necesita `onError` → state fallback**, renderizando img y placeholder mutuamente excluyentes (nunca simultáneos) y reseteando el flag al cambiar el `src`. Sin esto el browser deja visible el `alt` truncado dentro del avatar = layout shift. Canon escalado a `Spec/gotchas/ui-componentes.md` (post-F42.5).
- **Un placeholder de "procesando" que depende de una CF debe distinguir el estado offline**, donde la CF jamás corre por arquitectura (trigger requiere doc en Firestore → requiere red): el mensaje genérico "miente" y muestra un spinner indefinido. La heurística de red local es suficiente y precisa sin consultar la queue.
- **Consolidar dos componentes que expresan el mismo concepto (estado de red) reduce la carga cognitiva cross-platform**; el costo es ampliar el contrato del componente sobreviviente (acá: el chip pasa a persistir aún sin pending cuando hay offline, invirtiendo su early-return).
