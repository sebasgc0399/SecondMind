# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Firebase (solo el cache local, no la config del proyecto)
.firebase

# Environment
.env.local
.env.*.local

# Claude Code (config local de la máquina, no compartir)
.claude/settings.local.json

# Playwright MCP — artefactos de snapshots y console logs de tests
.playwright-mcp/

# Tauri — build artifacts y mobile gen (dev only)
src-tauri/target/
src-tauri/gen/
