# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Firebase (solo el cache local, no la config del proyecto)
.firebase

# Environment
.env.local
.env.*.local

# Claude Code (config local de la máquina, no compartir)
.claude/settings.local.json

# Playwright MCP — artefactos de snapshots y console logs de tests
.playwright-mcp/

# Tauri — build artifacts y mobile gen (dev only)
src-tauri/target/
src-tauri/gen/

# Capacitor Android — build artifacts, caches y secretos locales
android/app/build/
android/build/
android/.gradle/
android/captures/
android/local.properties
android/app/release/
android/app/debug/
*.apk
*.aab
*.keystore
!android/app/debug.keystore
